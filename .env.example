# Arquivo de exemplo para variáveis de ambiente
# Copie este arquivo para .env e configure os valores apropriados

# === CONFIGURAÇÕES DE SEGURANÇA ===
# Chave secreta para sessões Flask (OBRIGATÓRIA PARA PRODUÇÃO)
SECRET_KEY=your-secret-key-here

# === CONFIGURAÇÕES DO BANCO DE DADOS ===
# URL do banco de dados (SQLite por padrão para desenvolvimento)
DATABASE_URL=sqlite:///servidor_app/app.db

# === CONFIGURAÇÕES DE DIRETÓRIOS ===
# Diretório raiz da aplicação
ROOT_DIR=D:/Servidor/

# Diretório onde os bancos de dados do XAMPP estão armazenados
DB_ROOT_DIR=D:/Servidor/xampp/mysql/data

# Diretório para listar os sistemas
SISTEMAS_DIR=D:/Servidor/xampp/htdocs

# === CONFIGURAÇÕES DE PRODUÇÃO (BANCO DE DADOS) ===
# Credenciais do banco de dados de produção
PROD_DB_HOST=localhost
PROD_DB_USER=your-prod-username
PROD_DB_PASSWORD=your-prod-password
PROD_DB_PORT=3306

# === CONFIGURAÇÕES LOCAIS (BANCO DE DADOS) ===
# Credenciais do banco de dados local (XAMPP/MySQL)
LOCAL_DB_USER=root
LOCAL_DB_PASSWORD=

# === CONFIGURAÇÕES DO NGROK ===
# Token de autenticação do ngrok (opcional, mas recomendado)
NGROK_AUTHTOKEN=your-ngrok-auth-token

# Autenticação básica para o túnel ngrok (formato: usuario:senha)
NGROK_BASIC_AUTH=admin:password123

# === CONFIGURAÇÕES DE DESENVOLVIMENTO ===
# Modo debug (true para desenvolvimento, false para produção)
FLASK_DEBUG=true

# Porta do servidor Flask
FLASK_PORT=5000

# === NOTAS DE SEGURANÇA ===
# 1. NUNCA commite o arquivo .env no Git
# 2. Use senhas fortes e únicas
# 3. Para produção, configure todas as variáveis obrigatórias
# 4. Considere usar um arquivo .my.cnf para credenciais MySQL
# 5. Mantenha o NGROK_AUTHTOKEN seguro
